/*import 'dart:ffi';

import 'package:flutter/material.dart';
import 'package:medical/appcolors/colors.dart';

class R_Button extends StatelessWidget {
  Bool? isResponsive;
  Double? width;
  R_Button({Key? key, this.width, this.isResponsive}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: width,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(10),
        color: AppColors.mainColor,
      ),
      height: 60,
      child: Row(
        children: [
          Image.asset('assets/10.png'),
        ],
      ),
    );
  }
}*/
